/// A container of a key value name pair.
class Tag {
  Tag({
    required this.key,
    required this.value,
  });

  /// Name of the object key.
  final String key;

  /// Value of the tag.
  final String value;
}
